#include <stdio.h>

int n;
int k;
int A[100000];

int P(int x){
    return (long long int);
    int y;
    y=0;
    for(i=0;i<n;i++){
        y=
    }
}


int main(){
  int i, lb, ub;
  scanf("%d%d", &n, &k);
  for(i = 0; i < n; i++){
    scanf("%d", &A[i]);
  }
  

  return 0;
}
